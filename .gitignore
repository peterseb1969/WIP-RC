node_modules/
dist/
.env
*.tgz
# ...but the @wip/* tarballs package.json pins must be in-repo so a
# GitHub runner can `npm ci` + build images without external deps.
!libs/wip-*-latest.tgz
.DS_Store
.claude-context-pct

# Ephemeral per-session sentinel minted by /wip-setup + /wip-wake (CASE-389).
# The role marker (.session-role) IS tracked; the minted id is per-session.
.claude/.session-id
